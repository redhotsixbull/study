/**
 * 문제: 정렬된 배열에서 두 수의 합
 *
 * 정렬된 배열이 주어졌을 때, 두 수의 합이 target과 같은
 * 인덱스 쌍을 찾으시오. (1-based 인덱스)
 *
 * 예시:
 * 입력: numbers = [2,7,11,15], target = 9
 * 출력: [1,2] (numbers[0] + numbers[1] = 2 + 7 = 9)
 *
 * 시간 복잡도: O(n)
 * 공간 복잡도: O(1)
 */
export declare function twoSum(numbers: number[], target: number): number[];
export declare const testCases: {
    input: {
        numbers: number[];
        target: number;
    };
    expected: number[];
    description: string;
}[];
//# sourceMappingURL=01-two-sum-sorted.d.ts.map